<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Ember</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        {{#link-to 'application' tagName='li'}}<a href="">Home</a>{{/link-to}}
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            Tasks<span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            {{#link-to 'tasks' tagName='li'}}<a href="">List</a>{{/link-to}}
            {{#link-to 'tasks.new' tagName='li'}}<a href="">New</a>{{/link-to}}
          </ul>
        </li>
        {{#link-to 'posts' tagName='li'}}<a href="">Posts</a>{{/link-to}}
        {{#link-to 'profile' tagName='li'}}<a href="">Profile</a>{{/link-to}}
      </ul>
    </div>
  </div>
</nav>
<div class="container">
    <div class="row">
        <div class="col=md-12">
            {{ outlet }}
        </div>
    </div>
</div>
{{!-- The following component displays Ember's default welcome message. --}}
{{!-- {{welcome-page}} --}}
{{!-- Feel free to remove this! --}}
