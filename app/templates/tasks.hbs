<h1>Tasks</h1>
{{ outlet }}
{{#each model as |task|}}
    <div class="well">
        <h4>{{ task.title }}</h4>
        <small>Due: {{task.date}}</small>
        <p>{{ task.description }}</p>
    </div>
{{/each}}